// =========================================================
// SHeLL — renderer/sw_renderer.hpp
// =========================================================
// SDL2 software renderer backend (CPU-only fallback).
// Used when OpenGL 3.3 is not available (no GPU, no Mesa).
// Implements IRenderer using SDL_Renderer with
// SDL_RENDERER_SOFTWARE flag.
// =========================================================

#pragma once
