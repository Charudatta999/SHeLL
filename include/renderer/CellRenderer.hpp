// =========================================================
// SHeLL — renderer/cell_renderer.hpp
// =========================================================
// Instanced rendering of the terminal cell grid.
// Each cell (glyph + fg + bg + attrs) is rendered in a single
// draw call using OpenGL instancing for maximum throughput.
// =========================================================

#pragma once
